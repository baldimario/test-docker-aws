version: 0.2

env:
  variables:
    NODE_ENV: "development"

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install -g jest

  build:
    commands:
      - npm install
      - npm run test:ci

cache:
  paths:
    - './node_modules/**/*'